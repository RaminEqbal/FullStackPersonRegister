
/**
 * Simple Topbar for this Project
 */
const HeadBar = (props) => {

    return (
        <div className="navbar">
            <h1>{props.title}</h1>
        </div>
    )

}

export default HeadBar;